@model TodoItem

<!-- remember that convention dictates that the asp attributes are trimmed of file path
     so in asp-action, it is truncated from /Todo/AddItem -->
<form asp-action="AddItem" method="POST">
    <label asp-for="Title">Add a new item: </label>
    <input asp-for="Title">
    <button type="submit">Add</button>
</form>